[project]
name = "spa-template"
version = "0.1.0"
description = "No-build static SPA template with Playwright e2e and GitHub Pages deploy."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-playwright>=0.5",
]
